// Configuration shim: the oracle record is the per-configuration cycle
// image. Each variant is generated by
// duts/boom-repcut/scripts/generate_system_oracle_record.py from its
// manifest and qualification.
#pragma once
#if defined(CHISA_ORACLE_RECORD_HEADER)
// Experiment override: a producer built against another contract (subset /
// oracle-budget experiments) names its own generated record header.
#include CHISA_ORACLE_RECORD_HEADER
#elif defined(BOOM_GUIDER_CFG_LARGEBOOM_RC)
#include "system_oracle_record_large.h"
#else
#include "system_oracle_record_small.h"
#endif
