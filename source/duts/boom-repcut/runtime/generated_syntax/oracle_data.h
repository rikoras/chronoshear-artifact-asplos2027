#pragma once

// A generated binding check does not execute an oracle-bearing window.  The
// real live harness supplies its own window-major storage through
// TestHarness::oracle_win_base; this empty file only satisfies the generated
// header's unconditional include while ORACLE_NUM_CYCLES is defined as zero.
