#pragma once

// The W=1 validation image is emitted with an exact zero-oracle contract.
// TestHarness.h includes this file unconditionally; runtime oracle storage is
// intentionally absent and the W4 tap inventory is sampled independently.
